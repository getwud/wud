---
title: Version 8.x
description: Release notes and changelog for WUD version 8.x series.
---

# Version 8.x

Release history and details for the **WUD 8.x** series.

## [8.4.0](https://github.com/getwud/wud/releases/tag/8.4.0) · <small>September 3, 2026</small>

- 🚀 `AUTH` Complete login screen redesign: modern layout with rounded cards, fluid input styles, password visibility toggle, and responsive themes consistent with the rest of the application
- 🚀 `AUTH` Add automatic redirect to OIDC provider when only OIDC authentication is configured
- 🚀 `AUTH` Preserve and restore the initially requested URL (`next`) across both Basic and OIDC authentication flows
- 🚀 `DOCS` Expand OpenID Connect documentation with integration guides for Keycloak, Okta, Authentik, Authelia, Auth0, and generic OIDC providers
- 🚀 `TRIGGER` Add AMQP / RabbitMQ trigger (AMQP 0-9-1 broker support)
- 🚀 `TRIGGER` Add NATS trigger (NATS messaging system)
- 🚀 `TRIGGER` Add GitHub Actions trigger (repository_dispatch events)
- 🚀 `TRIGGER` Add GitLab CI trigger (Pipeline Trigger API)
- 🚀 `TRIGGER` Add Opsgenie trigger (Atlassian Alerts API)
- 🚀 `TRIGGER` Add PagerDuty trigger (Events API v2)
- 🚀 `TRIGGER` Add Uptime Kuma trigger (Push Monitors)
- 🚀 `TRIGGER` Add Signal trigger (via signal-cli REST API)
- 🚀 `TRIGGER` Add WhatsApp trigger (via Meta WhatsApp Cloud API)
- 🚀 `TRIGGER` Add Bark trigger (iOS Push Notifications)
- 🚀 `TRIGGER` Add Home Assistant webhook trigger
- 🚀 `TRIGGER` Add Prowl trigger (iOS Push Notifications)
- 🚀 `TRIGGER` Add Matrix trigger (Client-Server API)
- 🚀 `TRIGGER` Add Mattermost trigger (Incoming Webhooks)
- 🚀 `TRIGGER` Add Zulip trigger (Streams and Direct Messages API)
- 🚀 Skip tag listing for non-semver images
- 🚀 `MQTT` Add customizable Home Assistant device ID and device name
- 🚀 `NOMAD` Add Nomad trigger
- 🚀 `REGISTRIES` Refactor registries to leverage Docker Registry v2 inheritance and standardize unit tests
- 🚀 `REGISTRIES` Add support for Alibaba Cloud (ACR), DigitalOcean (DOCR), Harbor, IBM Cloud (ICR), Inedo ProGet, JFrog Artifactory, Linode, Oracle Cloud (OCIR), Scaleway, and Sonatype Nexus
- 🚀 `UI` Standardize icon rendering with Iconify (`@iconify/vue`), enabling access to over 150,000+ icons (Material Design, Simple Icons, Selfh.st, SVG Logos, Font Awesome, etc.)
- 🚀 `UI` Complete visual redesign: modern SaaS layout with flattened sidebar navigation, streamlined full-height viewports, refined color palette harmonized with the documentation, and responsive dark/light themes
- 🚀 `UI` Modernize toast notifications (`SnackBar`) with contextual icons, semantic alert colors, top-right positioning, and compact close action
- 🚀 Migrate doc from Docsify to Docusaurus
- 🐛 Fix login redirect issues when WUD is exposed under a subpath
- 🐛 `HTTP` Mask HTTP auth password and bearer token values
- 🐛 Preserve update results across registry errors
- 🐛 `REGISTRIES` Bound concurrent registry requests and retry transient HTTP 429 responses
- 🐛 `DOCKER-COMPOSE` Prefer trigger-level configuration over automatic labels
- 🐛 `UI` Fix dynamic chunk loading failure (`ChunkLoadError`) when navigating between nested configuration routes
- ⚠️ `UI` Deprecate Homarr icon prefix (`hl:`, `hl-`) in favor of `selfhst:` (automatically mapped with a deprecation warning)
- 🔧 `UI` Update dependencies to latest versions (Vue 3, Vuetify 3, Vue Router 4) and remove unnecessary packages
- 🔧 `UI` Refactor components into Single File Components (SFCs) by merging separate `.ts` scripts into `.vue` files
- 🔧 `E2E` Modernize UI end-to-end Playwright tests with improved selectors and automatic environment cleanup
- 🔧 `E2E` Add lightweight mock OIDC service and Cucumber test coverage for OIDC authentication and redirection endpoints
- 🔧 `CI` Migrate CI workflow from Travis-CI to GitHub Actions
- 🔧 `CI` Add automated UI E2E testing job running with containerized dependencies

---

## [8.3.1](https://github.com/getwud/wud/releases/tag/8.3.1) · <small>July 31, 2026</small>

- 🚀 Add `WUD_REGISTRY_HUB_PUBLIC_SUPPRESSDIGESTWATCHWARNING` environment variable
- 🚀 `GITLAB` Add support for GitLab group access tokens
- 🐛 `DOCKER-COMPOSE` Fix trigger failing to detect containers in compose file
- 🐛 `PROMETHEUS` Reduce CPU usage
- 🐛 `OIDC` Fix issues when WUD starts while OIDC provider is temporarily unavailable
- 🐛 `DOCKER` Fix Docker container update when container is attached to multiple networks
- 🐛 `UI` Fix card overflow on home page
- 🔧 `TELEGRAM` Replace deprecated client with direct HTTP API calls

---

## [8.3.0](https://github.com/getwud/wud/releases/tag/8.3.0) · <small>July 14, 2026</small>

- 🚀 Add opt-in mode for trigger association
- 🚀 `TELEGRAM` Add SOCKS5/HTTP proxy support to Telegram trigger
- 🚀 Add runtime subpath proxy support with `WUD_SERVER_BASEPATH`
- 🚀 Add environment variable for default digest watching
- 🚀 `MQTT` Improve trigger performance and reliability
- 🚀 `DISCORD` Add avatar URL support
- 🐛 Fix digest comparison for single-platform manifests resolved from a manifest list
- 🐛 `NTFY` Fix basic authentication
- 🐛 `UI` Fix container filters on mobile devices
- 🐛 `ECR` Use link header for pagination
- 🐛 Fix Passport authentication handling
- 🐛 `DOCKER-COMPOSE` Fix handling of services defined without images
- 🐛 Fix text log format output
- 🔧 Update OIDC library dependencies
- 🔧 Add multi-stage UI build to Dockerfile
- 🔧 Add Playwright end-to-end tests

---

## [8.2.2](https://github.com/getwud/wud/releases/tag/8.2.2) · <small>February 26, 2026</small>

- 🚀 Add public Codeberg registry (`codeberg.org`) to default supported registries
- 🚀 Add public Forgejo registry (`code.forgejo.org`) to default supported registries
- 🐛 Fix startup errors encountered on specific system configurations

---

## [8.2.1](https://github.com/getwud/wud/releases/tag/8.2.1) · <small>February 25, 2026</small>

- 🐛 `APPRISE` Fix bad request error (*"Payload lacks minimum requirements"*)
- 🐛 `DISCORD` Fix bad request error (*"Invalid URL"*)
- 🐛 `NTFY` Fix token authentication
- 🐛 Fix metrics-related errors when Prometheus is disabled
- 🐛 Fix `wud.watch.digest` configuration not being respected
- 🔧 Migrate backend codebase to TypeScript

---

## [8.2.0](https://github.com/getwud/wud/releases/tag/8.2.0) · <small>February 14, 2026</small>

- 🚀 Add TrueForge Container Registry support (`oci.trueforge.org`)
- 🚀 Add Codeberg registry support
- 🚀 Allow disabling Prometheus metrics
- 🚀 Enable digest watching by default (except for Docker Hub images)
- 🚀 Ensure candidate tags retain the same number of semver parts
- 🚀 Ensure candidate tags retain the same prefix
- 🚀 Add support for `wud.compose.file` label
- 🚀 Add Rocket.Chat trigger
- 🚀 `SMTP` Allow `from` address to include a display name
- 🐛 Fix Docker Compose YAML parsing when multiple aliases are used
- 🐛 Ignore `.sig` signature tags during candidate version resolution
- 🔧 `UI` Migrate UI to Vue 3, Vuetify 3, and TypeScript
- 🔧 Upgrade runtime to Node.js 24 and switch to Alpine Linux base image

---

## [8.1.1](https://github.com/getwud/wud/releases/tag/8.1.1) · <small>July 6, 2025</small>

- 🐛 `TELEGRAM` Fix Markdown character escaping in notification messages

---

## [8.1.0](https://github.com/getwud/wud/releases/tag/8.1.0) · <small>July 4, 2025</small>

- 🚀 Add 60s default jitter to Docker watcher to prevent load spikes on Docker Hub
- 🚀 `SMTP` Add support for custom TLDs in SMTP trigger
- 🚀 `TELEGRAM` / `SLACK` Add custom title configuration to triggers
- 🚀 `UI` Add support for [Homarr Labs](https://github.com/homarr-labs/dashboard-icons) icons
- 🚀 `UI` Add support for sorting containers by oldest creation date
- 🐛 Fix `${prerelease}` variable evaluation in link templates

---

## [8.0.1](https://github.com/getwud/wud/releases/tag/8.0.1) · <small>January 21, 2025</small>

- 🚀 Force watcher to check on startup only if store is empty ([#570](https://github.com/getwud/wud/issues/570))
- 🐛 Fix default healthcheck when HTTP server is disabled ([#556](https://github.com/getwud/wud/issues/556))
- 🐛 Fix missing Prometheus label ([#562](https://github.com/getwud/wud/issues/562))
- 🐛 `DOCKER-COMPOSE` Fix manual container update ([#546](https://github.com/getwud/wud/issues/546))

---

## [8.0.0](https://github.com/getwud/wud/releases/tag/8.0.0) · <small>January 10, 2025</small>

:::warning[**Breaking changes!**]
- **Registry Configuration**: Registry configuration structure has changed. Please update [your registry environment variables](../configuration/registries/README.md).
- **Storage State Reset**: Internal IDs have changed; your [existing state / storage](../configuration/storage/README.md) will be reset upon upgrading.
:::

- 🚀 `COMMAND` Add support for [Command](../configuration/triggers/command/README.md) trigger
- 🚀 `DOCKER` Add default healthcheck to the `wud` Docker image
- 🚀 `PUSHOVER` Add support for optional message TTL
- 🚀 `REGISTRY` Add support for configuring multiple registries of the same type
- 🚀 `TRIGGER` Add support for automatic or manual trigger execution modes
- 🚀 `TRIGGER` Improve `title`, `body`, and `link` template variables
- 🚀 `UI` Add ability to group containers by label
- 🚀 Brand new WUD logo! 🎉
- 🐛 `TRIGGER` Fix association issue between specific triggers and containers
- 🔧 Add Prettier code formatting and upgrade to Node.js 23
