{
  "name": "wud-e2e",
  "version": "1.0.0",
  "description": "WUD - e2e",
  "main": "index.js",
  "scripts": {
    "lint": "eslint '**/*.js'",
    "cucumber": "cucumber-js features/**/*.feature --tags 'not @skip and not @kubernetes'",
    "cucumber:local": "cucumber-js features/**/*.feature --tags 'not @ci-only and not @skip and not @kubernetes'",
    "cucumber:kubernetes": "cucumber-js features/kubernetes.feature --tags @kubernetes",
    "test:local": "LOCAL_MODE=true dotenvx run -- ../scripts/run-e2e-tests.sh",
    "test:ci": "CI=true dotenvx run -- ../scripts/run-e2e-tests.sh",
    "test:kubernetes": "dotenvx run -- ../scripts/run-k8s-e2e-tests.sh"
  },
  "author": "fmartinou",
  "repository": "getwud/wud",
  "license": "MIT",
  "dependencies": {
    "@cucumber/cucumber": "11.1.1",
    "apickli": "3.0.3",
    "mqtt": "5.14.1"
  },
  "devDependencies": {
    "@dotenvx/dotenvx": "^1.51.4",
    "eslint": "8.57.0",
    "eslint-config-airbnb-base": "15.0.0",
    "eslint-plugin-cucumber": "2.0.0",
    "eslint-plugin-import": "2.30.0"
  }
}